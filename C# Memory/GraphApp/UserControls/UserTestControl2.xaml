<model:NotifyPropertyChangedUserControl  
             xmlns:model="clr-namespace:GraphApp.Model"
             x:Class="GraphApp.UserControls.UserTestControl2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GraphApp.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="{Binding Path=TextCommon, UpdateSourceTrigger=PropertyChanged}"/>

        <!--<Label Grid.Row="0">
    <Label.Content>
        <MultiBinding Converter="{StaticResource BoolToTextConverter}">
            <Binding Path="BoolProp" />
            <Binding Path="TextProp1" />
            <Binding Path="TextProp2" />
            </MultiBinding>
            </Label.Content>
        </Label>-->

    </Grid>
</model:NotifyPropertyChangedUserControl>
