<UserControl  
             xmlns:model="clr-namespace:GraphApp.Model"
             x:Class="GraphApp.CustomControls.UserTestControl2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Button  Click="Button_Click"/>
            <TextBox Grid.Row="1" Text="{Binding Path=Text1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <!--<Label Grid.Row="2" Content="{Binding Path=Text1}"/>-->
        </Grid>
    </Grid>
</UserControl>
